# RTS Fusion Engine Plan

Date: 2026-06-12

This plan turns the current heavy `trnm-world-bevy` RTS implementation into module boundaries that can absorb direct internal AGPL/GPL imports where that is faster, while keeping a future replacement path for public release or hosted service.

## Direction

Do not start a separate rewrite. Keep the current playable Bevy RTS on `main`, then cut the heaviest surfaces into import-friendly crates:

- `trnm-rts-core`: deterministic world, actors, orders, fog, occupancy, resources, replay hashes.
- `trnm-rts-data`: maps, rules, tiles, spawns, asset manifests, OpenRA/TrillionniumRTS-style YAML-derived data.
- `trnm-rts-bevy-runtime`: camera, input, minimap, terrain renderer, movement/path preview, Bevy adapter.
- `trnm-rts-online`: shared world/protocol/update envelope, arena lifecycle, visibility-scoped updates, bots.
- `trnm-rts-evidence`: screenshots, JSON gates, PPM helpers, release-review proof generation.

The first cut has landed. The plan is now in extraction and Bevy-facing consumption mode rather than crate-bootstrap mode.

## Current Status - 2026-06-24

The following boundaries are already present on `main`:

- `trnm-rts-core` exists with Bevy-free frame orders, validation, deterministic headless replay checkpoints, and rule-stream evidence used by live input, economy/build, combat, bot, fog, and tactical-micro proof surfaces. It is a useful deterministic contract layer, but the full authoritative RTS world is not fully migrated there yet.
- `trnm-rts-data` exists with `RtsMapModel`, typed First Contact Basin players, rules, actors, bounds, source manifest, and deterministic canonical hash.
- `trnm-rts-data` also owns the First Contact renderer-neutral map projection through `RtsMapRendererModel` and `first_contact_map_renderer_model(&RtsMapModel)`. It now owns the First Contact preview actor projection through `RtsFirstContactPreviewActor`, `RtsFirstContactPreviewActorKind`, and `first_contact_preview_actors(&RtsMapModel)`. Bevy consumes those projections for terrain/resource/base/objective/spawn/minimap evidence and OpenRA-like preview actors instead of keeping Bevy-local actor tables or rule-kind adapters. The 2026-06-24 codegen-boundary work also moved renderer-neutral First Contact sample/signature/classifier data into `trnm-rts-data::first_contact_samples` and compact player-facing label helpers into `trnm-rts-data::first_contact_labels`.
- `trnm-rts-bevy-runtime` exists and owns deterministic camera, minimap, projection, path-preview, tile-line, hit-test, runtime layout calculations, the First Contact player-screen runtime application contract, the First Contact offline-adapter runtime application contract, the First Contact offline-adapter lobby-ready review contract, the First Contact offline-adapter session-transition review contract, and the First Contact offline-adapter player-screen consumption review contract.
- `trnm-rts-evidence` exists and assembles deterministic Bevy runtime adapter evidence before `trnm-world-bevy` includes that proof in release-review evidence. It also owns the campaign UI continuity review contract for campaign handoff final/restored route state, save-restore persistence, milestone pixels, native-client no-credit boundary, and player-first route-resume screen gates. It now also owns the session-state continuity review contract for save-slot confirmation, load-resume lock/continue, recovery guard, match setup, restored HUD, campaign outcome, campaign continuity, source preview readiness, native-client no-credit boundaries, and the player-first session resume screen. It now owns the continuous player-flow review contract for the six-step title/account -> match setup -> in-match HUD -> command feedback -> save/resume -> outcome/open-world local runtime surface, including player-first screen and no-credit boundaries. It also owns the live-session playthrough review contract for the same-process trace sidecar, live RTS command acceptance, slot A save/load/resume, final open-world handoff, player-first tactical screen, and no-credit boundary. It now owns the full-game visual/UI aggregate review for source contracts, nested evidence reviews, 18 UI coverage surfaces, live-session/open-world handoff, player-first tactical pixels, runtime screen chain, role-readable command-grid state samples, and no-credit boundaries. It now owns the OpenRA-style screen-set review for widget roots, eight reference screens, interaction surfaces, player-first ingame HUD pixels, source artifact chain, and no-copy/no-overclaim boundaries. The release-review packet now also carries a `trnm_rts_evidence_release_review_packet_assembly_review_v1` object that reviews manifest completeness, missing-artifact state, key RTS runtime artifacts, packet semantic fixtures, status handoff, public-launch no-credit boundary, and external blocker state before reviewer handoff. The 2026-06-24 codegen-boundary work moved First Contact visual, radar, spatial, silhouette, art, motion, marker-budget, selection/combat-focus, target-callout, atlas-readability, and command-grid readability JSON/gate summarizers into `trnm-rts-evidence`; Bevy keeps stable wrapper names and renderer-owned draw paths, colors, geometry, asset loading/sizing, and live pixels. Marker-budget now uses an explicit Bevy-free focus-geometry plus frame-pixel-area snapshot instead of a live frame-size closure; selection/combat-focus and target-callout now use an explicit Bevy-free label-width, route/target tile, and focus-geometry snapshot; atlas-readability now uses an explicit asset-manifest, frame-availability, override-frame ID, and frame-pixel-area snapshot; command-grid readability now uses a Bevy-free active-ability and cooldown snapshot while keeping glyph draw/backdrop colors in Bevy.
- `trnm-rts-online` exists as a no-socket deterministic protocol fixture for authority resolution, visibility-scoped updates, loopback transport frames, bot plan, arena lifecycle, a Bevy-facing local handoff summary, and an offline loopback adapter summary for local multiplayer/bot handoff. The offline adapter now also carries dedicated local UI/action replay, runtime handoff, and Bevy-runtime review-input builders derived from retained/pruned control-group replay fixtures and server-authoritative accepted/rejected fixture orders. It keeps client-prediction, rollback-netcode, hosted-service, socket, and public-launch claims false.
- The First Contact Bevy spec now applies the `trnm-rts-data` player-screen profile through the Bevy-free `trnm_rts_bevy_runtime_first_contact_player_screen_application_v1` contract before mutating `NativeFirstPlayableRuntime`; it then consumes the no-socket offline adapter into local action-replay state through the `trnm-rts-online` review-input builder and `trnm_rts_online_offline_adapter_runtime_handoff_v1`, translates that handoff through the Bevy-free `trnm_rts_bevy_runtime_first_contact_offline_adapter_runtime_application_v1` contract, reviews local lobby/ready-state as `trnm_rts_bevy_runtime_first_contact_offline_adapter_lobby_ready_v1`, reviews the data-seeded command surface -> server-authoritative handoff as `trnm_rts_bevy_runtime_first_contact_offline_adapter_session_transition_v1`, and hands the resulting player-screen snapshot to `trnm_rts_bevy_runtime_first_contact_offline_adapter_consumption_v1` for Bevy-free review. The server-authoritative accepted move reaches the runtime command queue/stamp, and the fogged attack rejection is suppressed from UI state. This is still local/offline proof only, not network readiness.
- The same First Contact proof now extends that offline adapter consumption into a visible player-screen/session handoff surface: room/map, camera, visibility, queues, supply, objective state, and route overlay remain coherent while the accepted server move replaces the local command surface. This is still explicitly local, no-socket, no-hosted-service, no-client-prediction, no-rollback, and no-public-launch evidence.
- The 2026-06-19/21/22/23 work has shifted from boundary-only extraction back to the visible player screen because the highest product risk is still "does this look and read like a real RTS?" The live runner title is concise and player-facing, resource readouts render `CREDITS` / `POWER` / `SUPPLY` / `VISION` with spacing, camera tiles render as `5/4` in visible HUD text, active selected groups no longer display as zero units, production/order/build/completion labels use player-facing names, FIELD STATUS and TACTICS lanes render compact readable detail rows, the tactical header is split into compact title/order/camera blocks, build-placement status uses player-facing `PLACE ...` wording below the header, the First Contact map has selected-squad rings, route marks, objective/structure/lane readability cues, the radar now carries visibility/fog/route/objective/structure/pressure/viewport context, the command grid now has six role-specific glyph signatures instead of repeated generic buttons, the full-game visual/UI aggregate records role-readable command-grid samples instead of trusting generic button repetition, the right sidebar now compacts production-slot names and readiness/status into fixed `GRD` / `WRK` / `SIG` / `TRN` plus `RDY` / `Q1` / `B2` / `ADD` badges, build-palette slot names into fixed `PWR` / `TRN` / `REF` / `TWR` / `CMD` / `RAD` / `WAL` / `SIG` badges and READY/QUEUE state into fixed RDY/QUE badges, and TACTICS dynamic values into fixed `SECURE` / `BEACON` / `G64/T42` badges plus queue fallbacks such as `TRN SIG` / `BLD RLY` / `ATK BCN` so truncated live values like `TRAIN SI` cannot leak, with guarded production-to-palette and palette-to-tactics spacing, the bottom selection/status panel now renders concise squad/status information such as `GROUP 1 SECURING RELAY`, `SIGNAL BLADE READY`, `WATCH TOWER READY`, and `WORKER` / `SCOUT` / `GUARD` / `RELAY` role chips plus compact bottom order-queue badges such as `ATK BCN`, `BLD RLY`, `MOV 16/9`, and `SIG RDY` instead of raw RTS completion strings, the map body now has a dedicated silhouette pass for terrain zones, worker/scout/warden/relay unit shapes, command-core rooflines, relay masts, and beacon spires, the live player-screen screenshot probe now records separate dead-panel, clipped-label edge, gameplay-scene, and debug-title gates, the First Contact map now adds authored terrain material, building-facade, and map-landmark detail samples for base concrete seams, flux crystal shards, beacon-lane chevrons, cracked basin floor marks, command-core window rows, relay antenna panels, beacon spire glow panels, base gate lamps, crystal shadow sparkles, lane power pylons, crater scuff marks, relay ground cables, and beacon capture rings; the runtime actor glyph now adds guarded original structure depth details for roof rims, side shadows, command window pips, relay mast braces, and beacon core glow rungs; motion readability now proves opening action verbs, unit status roles, tactical tracks, tactical-track density budgets that keep the relay-to-beacon track hot while fading secondary tracks farther into the terrain at a 3/4 mix, command feedback progress, route/target continuity, combat event cues, plus visible animation-cycle detail frames for worker harvest/carry/footfall, scout sweep/turn, warden shield/recoil, relay packet pulses, command-core training/spawn, relay construction sparks, and beacon capture/rally, and the same real player-screen render path now places project-owned atlas frames and dedicated project-owned frame families for worker/scout/warden/relay unit roles, command-core/relay structure roles, and beacon objective roles over the First Contact map instead of relying only on primitive overlay shapes, with guarded atlas runtime grounding/depth underlays for unit shadows, structure footprint rims, and objective capture shadows. Those dedicated atlas frame-family samples now sit in north/west/east perimeter gallery lanes instead of the busy tactical core; their non-interactive gallery presentation is muted, and the lower-east beacon gallery samples now use single-point ghost anchors, suppressed shadows, stronger 5/6 dim silhouettes, and suppressed atlas depth underlays so role samples do not compete with command/attack/focus colors near the lower route lane. The opening command route now also has a visual hierarchy de-emphasis layer, a central clarity de-emphasis layer for non-focus core tiles, and a last-drawn selection/combat focus layer with route-clearance gutters, selected-unit corner brackets, compact route dash/ACK tick geometry, attack target lock brackets with a compact target-lock cross/ACK tick, a compact `BEACON 38%` target callout/health strip, and a blocked-tile warning cross so the key tactical action remains readable after the atlas/art/motion passes.
- `first_contact_player_screen_label_guard_v1`, `first_contact_visual_readability_v1`, `first_contact_radar_readability_v1`, `first_contact_command_grid_readability_v1`, `first_contact_sidebar_density_v1`, `first_contact_bottom_panel_readability_v1`, `first_contact_silhouette_readability_v1`, `first_contact_art_readability_v1`, `first_contact_motion_readability_v1`, `first_contact_atlas_readability_v1`, `first_contact_visual_hierarchy_v1`, `first_contact_central_clarity_v1`, `first_contact_terminal_legibility_v1`, `first_contact_selection_combat_focus_v1`, `first_contact_target_callout_v1`, and `first_contact_marker_budget_v1` now keep player-facing HUD labels, resource spacing, build-palette fit, order/completion labels and compact order-queue badges, tactical header title/order/camera fit, build-placement status wording/width, TACTICS/FIELD STATUS wording, compact TACTICS value badges, and compact TACTICS queue fallback badges, map readability overlays, radar context, command-grid role sequence, unique icon signatures, active-slot state, cooldown badge semantics, right-sidebar production/build-palette/tactics density, compact production slot-name/status, build-palette slot-name/status, and tactics status badges, bottom-panel feedback wording, selected-count density, squad-role chip fit/edge clearance, terrain zone sample signatures, unit role silhouettes, structure rooflines, beacon spires, terrain material roles, building-facade signatures, map-landmark roles/signatures, runtime actor depth signatures, opening action sequence, unit status roles, tactical track roles and density signatures including terrain-faded secondary tracks, command feedback progress, runtime combat cue continuity, animation-cycle frame signatures, project-owned atlas frame usage, dedicated atlas frame-family usage, atlas runtime depth signatures, atlas frame-family perimeter placement, muted non-interactive gallery marker/color presentation, lower-lane gallery single-point ghost-anchor/5-6 dim-silhouette de-emphasis with suppressed shadows and suppressed atlas depth underlays, opening-route visual hierarchy de-emphasis, center-core quiet-tile negative space, route-terminal quiet bands around target/blocked endpoints, opening-route clearance gutters, compact route ACK tick geometry, opening-route selection/combat focus markers with compact selected-role pips, compact final target-lock cross/ACK tick geometry, and compact target callout label/health-strip geometry under First Contact spec, packet integrity, v2 contract, and fixture coverage. The full-game visual/UI replication gate now separately requires role IDs, icon signatures, active/sent/available slot counts, and an active `signal` command-grid sample so the aggregate screenshot proof cannot pass with samey placeholder slots; the live runner status gate now rejects empty/dead HUD panels, likely clipped-edge labels, debug/proof title fragments, and non-gameplay-looking map/HUD screenshots before release packet review can pass.
- The 2026-06-23 terrain-material polish now adds guarded low-key depth underlays for base foundation bevels, flux-crystal cast shadows, recessed beacon-lane rails, and basin fracture shadows before the brighter authored map-art pass. This keeps the renderer-owned material work original and local while preserving the later route/selection/combat focus layers.
- The 2026-06-23 animation-frame polish now adds guarded secondary pose offsets, contact-smear ticks, structure shutter frames, and objective afterglow frames on top of the existing First Contact animation samples. This raises runtime frame richness without increasing tactical-track density or changing local/offline gameplay claims.
- The target-callout polish now also guards a small clearance gutter behind the `BEACON 38%` callout/leader, caps the earlier pre-focus target acquisition cue to two thin/dim rings plus a shorter cross, and keeps the final target lock compact with a guarded `28x3` cross plus `18x4` ACK tick. The target bracket, route, health strip, and focus layer remain preserved; this is a center-combat clutter reduction, not a new gameplay claim.
- The 2026-06-24 work switched back to engineering-cost and codegen-boundary risk after the obvious visible First Contact guard surfaces were split. Same-crate helper extraction made `trnm-world-bevy/src/lib.rs` easier to reason about, but profiling showed a forced binary-only release rebuild still spends roughly 10 minutes and about 3.4GB RSS in the giant `trnm-world-bevy` main crate. The safe mitigation already landed for the desktop playtest runner: release runs build only `--bin trnm-world-bevy` and no longer churn the Android `.so`; the real longer-term mitigation is to keep moving stable renderer-neutral data/evidence domains into `trnm-rts-data` and `trnm-rts-evidence` while leaving pixels, asset loading/sizing, colors, hit-testing, and draw-path math in Bevy.
- Release-review CI is green on the local release artifact path with 377 checks, 0 packet-integrity failures, and 128 packet artifacts after the First Contact readability and evidence-boundary slices. The current dominant local test-runtime costs are live-window screenshot evidence, packet semantic fixtures, and packet integrity; the current dominant rebuild cost is `trnm-world-bevy` release codegen. Live-window screenshot evidence now emits capture/readiness diagnostics so future speed work can distinguish window readiness, frame capture retries, and settle/key retries before trimming any proof.
- `public_launch_ready=false` and `android_s5_real_device_claimed=false` remain correct. They are blocked by real S5 device evidence, production map-pack public evidence, beta cohort evidence, commercial drill evidence, multi-node/live-traffic latency evidence, and public network exposure evidence. Public-launch blocker consistency now exposes explicit `green` and six-blocker fields, and operator handoff requires both before issuing the external-evidence handoff.

## Reference Ownership

### OpenRA

- Role: rules semantics, actor/trait model, order/frame discipline, YAML map/rules shape, fog, production, replay/sync.
- Import policy: mostly clean-room semantics; direct GPL snippets require explicit internal component tracking.
- Main reason: OpenRA is the best rules vocabulary reference, not the best Rust runtime.

### Digital Extinction

- Role: Bevy runtime structure: terrain, map, pathing, movement, camera, controller, minimap, construction, combat, lobby/network crate boundaries.
- Import policy: direct internal AGPL import is allowed when it materially speeds the work. Imported code must be isolated under an AGPL-marked crate/path and recorded in the source manifest.
- Main reason: it is the closest Rust/Bevy RTS runtime reference.

### Kiomet

- Role: large-world/shared-state architecture: common world/protocol, chunked world, visibility-scoped updates, server authority, bots.
- Import policy: direct internal AGPL import is allowed for protocol/world/visibility ideas when isolated; rendering/client assumptions must not become gameplay authority.
- Main reason: it is the most complete strict Rust online RTS reference, but not a Bevy/3D renderer.

### Kodiak

- Role: arena service boundary, binary socket envelope, WebSocket/WebTransport, lockstep/prediction/checksum framework.
- Import policy: LGPL component tracking before direct dependency or copied code.
- Main reason: Kiomet's architecture is incomplete without Kodiak.

### TrillionniumRTS

- Role: local content seed: First Contact Basin `map.yaml`, Trillionnium rules, placeholder art slots.
- Import policy: direct internal GPL-derived data is allowed and tracked because this OpenRA Mod SDK prototype is local and already isolated.
- Main reason: it gives an immediate map/rules data source for the Bevy player screen.

## Import Lanes

### Lane A: Direct Internal Import

Use this for AGPL/GPL/LGPL modules that can accelerate local development before public release.

Requirements:

- Record upstream/local path, commit, license, copied/derived status, local destination, and release constraint.
- Keep imports behind Trillionnium traits/interfaces so they can be replaced.
- Do not bundle Westwood, EA, Warcraft III, or other proprietary game data.
- Mark public launch and hosted-service readiness false until license obligations are reviewed.

### Lane B: Clean-Room Semantics

Use this for OpenRA rules/order concepts and any module whose direct import would bind too much of the mainline.

Requirements:

- Document upstream file/behavior read.
- Implement Trillionnium-owned types and tests.
- Keep compatibility evidence honest: no OpenRA binary replay/runtime parity claim unless proven.

### Lane C: Project-Owned Data

Use this for Trillionnium-owned rules, maps, and art generated specifically for this project.

Requirements:

- Keep source paths and generation provenance.
- Add asset manifests before bundling new art.
- Prefer typed Rust data models over ad hoc strings.

## Execution Slices

### Landed

The initial slice created `trnm-rts-data`:

- `RtsMapModel` for First Contact Basin.
- Typed players, rules, map actors, bounds, source manifest, and deterministic summary hash.
- Direct internal derivation from `TrillionniumRTS/mods/trnm/maps/first-contact-basin/map.yaml` and `mods/trnm/rules/trnm.yaml`.
- Tests for map size, playable bounds, players, actors, spawns, resources, objectives, expansion markers, and source manifest.

Follow-up slices have also landed enough to change the plan:

- Bevy First Contact spec/evidence consumes `trnm-rts-data`.
- First Contact terrain/resource/base/objective/spawn/minimap projection is derived in `trnm-rts-data`, with Bevy consuming `RtsMapRendererModel`.
- OpenRA-like preview map actors are derived from `first_contact_preview_actors(&first_contact_basin_map())`; spec and packet guards reject Bevy-local First Contact actor tables or rule-kind adapter regressions.
- Runtime adapter math and fixtures are split into `trnm-rts-bevy-runtime`.
- Release-review evidence assembly is split into `trnm-rts-evidence`, including the campaign UI continuity handoff review, session-state continuity review, continuous player-flow review, live-session playthrough review, full-game visual/UI aggregate review with role-readable command-grid state, OpenRA-style screen-set review, and release-review packet assembly review.
- No-socket online protocol and authority fixtures are split into `trnm-rts-online`, with `RtsOnlineLocalHandoff` and `RtsOnlineOfflineAdapterSummary` exposing the local Bevy-facing handoff/adapter contracts while keeping socket/hosted-service/public-launch flags false.
- Bevy First Contact spec consumes the `trnm-rts-data` player-screen profile through the `trnm-rts-bevy-runtime` player-screen runtime application before touching `NativeFirstPlayableRuntime`. It also consumes the offline adapter into a local action-replay summary, proving accepted server orders mutate the Bevy runtime command state while rejected fogged commands stay out of the UI/action queue. The upstream offline adapter contract now exposes accepted/blocked control-group replay inputs, bounded history semantics, server-authoritative runtime handoff labels/stamp, and runtime review-input builders before Bevy renders them; `trnm-rts-bevy-runtime` translates those labels/stamps into the runtime application consumed by `NativeFirstPlayableRuntime`, reviews local lobby/ready-state and the broader session/UI transition from data-seeded command surface to server-authoritative handoff, then owns the player-screen/session handoff review gates.
- `trnm-rts-core` now provides the Bevy-free frame-order stream and headless replay checkpoints used by multiple local RTS proof surfaces. It is landed as a contract/replay layer, while the plan still treats fuller deterministic world authority migration as future work.
- Shared release-review acceptance writers are serialized with a common `flock` helper, release-review CI exposes explicit check-count aliases, live-window screenshot evidence exposes capture/readiness diagnostics, and public-launch blocker consistency/operator handoff now expose machine-readable green/blocker-count state without claiming public launch.
- Visible First Contact player-screen polish has landed for title-bar wording, resource readout spacing, camera-tile formatting, selected-unit display count, production/order/build/completion labels, tactical header title/order/camera separation, build-placement status wording/placement, FIELD STATUS and right-rail TACTICS wording with compact TACTICS value badges and queue fallback badges, right-sidebar build-palette density, compact `PWR` / `TRN` / `REF` / `TWR` / `CMD` / `RAD` / `WAL` / `SIG` slot-name badges, and compact RDY/QUE state badges, bottom order-queue `ATK` / `BLD` / `MOV` / `SIG RDY` badges, map/radar readability context, command-grid icon differentiation, full-game aggregate command-grid readability, bottom selection/status panel readability, terrain/unit/building silhouette readability, live screenshot dead-panel/clipped-label/gameplay-scene/debug-title probing, authored terrain/building art readability, deeper map-landmark detail readability, richer unit/building/objective animation-cycle readability, project-owned atlas-backed terrain/unit/structure/objective frame samples, dedicated project-owned atlas frame families for worker/scout/warden/relay units, command-core/relay structures, and beacon objectives, and screenshot-informed opening-route visual hierarchy, central clarity, terminal legibility, selection/combat focus, target callout, route-clearance gutters, lower-lane gallery de-emphasis, muted secondary tactical tracks, and muted gallery marker-budget layers in the real First Contact player-screen render path. The dedicated atlas frame families now use north/west/east perimeter gallery lanes, explicitly avoid the busy tactical core, and use muted slot cues/darkened gallery frames so non-interactive samples do not compete with hot command colors; the lower-east beacon gallery samples are additionally dimmed at 5/6, suppressed into silhouettes, and shortened into single-point ghost anchors so they do not crowd the lower route lane. The First Contact label, visual, radar, command-grid, sidebar-density, full-game visual/UI, bottom-panel, silhouette, art-readability, motion-readability, atlas-readability, visual-hierarchy, central-clarity, terminal-legibility, selection/combat-focus, target-callout, marker-budget, and live-runner guards plus packet integrity checks now reject raw debug/internal display fragments, generic/samey command-grid regressions, cramped right-sidebar production/build-palette/tactics regressions, missing compact target labels/health strips, missing compact bottom order-queue badges, raw completion/status text in those lanes, tactical header or placement-status regressions that overlap, overflow, or leak raw internal markers, map-object regressions that collapse worker/scout/warden/relay units or command-core/relay/beacon structures back into samey blocks, terrain/building art regressions that drop material/facade/landmark signatures, animation regressions that drop worker/scout/warden/relay/command-core/relay/beacon frame signatures, atlas regressions that drop project-owned terrain/unit/structure/objective frame usage, role-specific frame families, perimeter placement, muted gallery presentation, or lower-lane de-emphasis, visual hierarchy regressions that let perimeter gallery samples crowd the command corridor, central clarity regressions that mute focus tiles or remove center-core negative space, terminal-legibility regressions that remove target/blocked endpoint quiet bands, selection/combat focus regressions that hide the selected opening squad, route, route-clearance gutters, attack target, or blocked-tile warning, and live screenshots that are blank, panel-dead, edge-clipped, or not convincingly gameplay-like. The artifact command runner also leaves `TRNM_WORLD_BEVY_CLASSIC_PLAYER_SCREEN=0` by default unless a caller explicitly opts in, so source-artifact refreshes are not polluted by player-screen mode.

### Next Local Slices

1. Keep burning down visible playable-screen polish before adding more abstract proof. The best next target is another screenshot-informed pass on any remaining combat-target clutter or command/readiness text density the live screenshot still exposes, then add only guards that catch real player-facing regressions the live screenshot probe still misses.
2. Move remaining renderer-neutral First Contact command-surface, runtime UI, and evidence helper code out of `trnm-world-bevy/src/lib.rs` only when the code is materially duplicated, blocks product polish, or reduces the `trnm-world-bevy` codegen surface. `trnm-world-bevy` should keep pixels, assets, hit-testing, colors, asset sizing, and draw-path math; `trnm-rts-data`, `trnm-rts-online`, `trnm-rts-bevy-runtime`, and `trnm-rts-evidence` should keep typed data, protocol handoff, runtime math, snapshots, and review contracts.
3. Continue extending the Bevy-facing offline adapter path into broader local session/UI transitions only if the result becomes visible or removes Bevy-local semantic re-derivation. Keep it explicitly no-socket, no-hosted-service, no-client-prediction, no-rollback-netcode, and no-public-launch until real network evidence exists.
4. Keep reducing packet semantic fixture and packet integrity cost only when the same semantic artifact checks remain covered. Avoid replacing semantic negatives with checksum-only assertions.
5. Use the live-window capture/readiness diagnostics to identify whether the slow path is window startup, xwd/ffmpeg capture, frame-change settling, or repeated key attempts. Do not reduce frames or settle retries until the diagnostics show a cheaper path preserves the same evidence.
6. Keep public launch blocked until the six external evidence items are real and validator-green; templates, synthetic fixtures, local drills, and handoff manifests remain no-credit.

## Release Boundary

Internal development may use direct AGPL/GPL imports. Public distribution, external beta, hosted network service, commercial launch, or public source release require a fresh component review:

- AGPL network-source obligations for network-interactive modified programs.
- GPL distribution/source obligations for copied/derived GPL components.
- LGPL dynamic/static linking and modification obligations.
- CC BY-SA/OFL attribution/share-alike obligations for assets.

Until that review is green, keep `public_launch_ready=false` and avoid claims that imported third-party code/assets are proprietary Trillionnium-owned work.

CI and evidence boundary:

- Release-review speedups are valid only when they preserve the same artifact semantics. The current live-window evidence gate is slow by design because it captures runtime window proof; do not weaken it without a better readiness/capture signal.
- Scripts that write shared `acceptance/S6_public_launch/latest` artifacts must use the shared release-review acceptance lock or isolated temporary paths. Parallel evidence-bundle, release-packet, and integrity refreshes can create checksum drift when they bypass that discipline.
- Local green CI does not unblock public launch. The six public/S5 blockers above require real external evidence, and templates or synthetic fixtures must keep the readiness flags false.
