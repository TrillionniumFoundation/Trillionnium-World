[package]
name = "trnm-pouw"
version = "0.1.0"
edition = "2021"

[features]
default = []
real-tee-backend = []
real-zk-backend = [
    "dep:ark-bn254",
    "dep:ark-ff",
    "dep:ark-groth16",
    "dep:ark-relations",
    "dep:ark-serialize",
    "dep:ark-snark",
    "dep:rand",
]

[dependencies]
thiserror = "1"
sha2 = "0.10"
hex = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
ark-bn254 = { version = "0.5", optional = true, default-features = false, features = ["curve"] }
ark-ff = { version = "0.5", optional = true, default-features = false }
ark-groth16 = { version = "0.5", optional = true, default-features = false }
ark-relations = { version = "0.5", optional = true, default-features = false }
ark-serialize = { version = "0.5", optional = true, default-features = false }
ark-snark = { version = "0.5", optional = true, default-features = false }
rand = { version = "0.8", optional = true }
trnm-state = { path = "../trnm-state" }
trnm-types = { path = "../trnm-types" }

[dev-dependencies]
trnm-state = { path = "../trnm-state", features = ["test-utils"] }
