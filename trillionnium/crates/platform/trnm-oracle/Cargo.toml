[package]
name = "trnm-oracle"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
sha2 = "0.10"
hex = "0.4"
thiserror = "1"

[dev-dependencies]
serde_json = "1"
