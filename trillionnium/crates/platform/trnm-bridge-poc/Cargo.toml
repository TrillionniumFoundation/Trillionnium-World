[package]
name = "trnm-bridge-poc"
version = "0.1.0"
edition = "2021"
authors = ["Trillionnium Contributors"]
license = "MIT"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
trnm-types = { path = "../trnm-types" }

[dev-dependencies]
